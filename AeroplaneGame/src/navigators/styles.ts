import { StyleSheet } from 'react-native';
import { Colors } from '../theme/Variables';

export const styles = StyleSheet.create({
  icon: { height: 30, width: 30, color: Colors.textGray800 },
  iconDark: { color: Colors.primary },
});
